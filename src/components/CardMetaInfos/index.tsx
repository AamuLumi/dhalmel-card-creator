import { Card, CardBody } from '@nextui-org/react';

export default function CardMetaInfos(_: any) {
	return (
		<Card className="flex-shrink-0">
			<CardBody>Card Meta Info</CardBody>
		</Card>
	);
}
